#
# This file is automatically generated using launchdarkly-code-generation tool.
# Do not modify this file -- YOUR CHANGES WILL BE ERASED!
#

class LDFlags:
    def __init__(self, ldclient):
        self.ldclient = ldclient

    {{#each flags.items}}
    def {{snakeCase this.key}}(self, user):
        """
        {{#if this.name}}
        {{ this.name }}

        {{/if~}}
        {{#if this.description}}
        {{ this.description }}
        {{/if}}
        """

        return self.ldclient.variation('{{ this.key }}', user, {{{ defaultValue this "double" }}})
    {{#unless @last}}

    {{/unless}}
    {{~/each~}}
